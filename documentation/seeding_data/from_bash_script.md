# Seeding Data - Bash Script

The Lifeframe README.md has a detailed guide for seeding data in both systems. This is because both systems need to run and some data needs to exist on both sides to effectivly develop.

There is a bash script that will automate all of the seed steps, just make sure the docker containers are running and run:

`./seed.sh`
